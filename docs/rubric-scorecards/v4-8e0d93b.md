# NDIA hostile rubric scorecard — v4

## Evaluation identity

| Field | Recorded value |
|---|---|
| Review date | 2026-09-08 |
| Evaluated product revision | `8e0d93b9610c5542c976c5fc14faf7de788527e3` |
| Evaluated product tree | `65db33fc2b5e1010307c2b86a79160ecf17b08a8` |
| Evaluated branch | `replit-configuration`, rebased onto shared main |
| Shared-main baseline | `2ce85ec2bd2bf8b8725c57ff3a21591a55044d6d` (`Simplify navigation and preserve active route context`) |
| Identity confidence | High. Current scorecard/memory commits are documentation-only descendants, and product/config/test files were byte-compared to the recorded revision before this scorecard was written. |
| Runtime | Configured `UC7 Illuminate` Replit development workflow; Neo4j 5.26.1, FastAPI, Vite on port 8080 |
| Published deployment | No immutable production revision was established. No production credit is inferred. |
| Data state | Deterministic fixture seed with scenario data enabled; UN Security Council and OFAC connectors are optional live sanctions sources |
| Required runtime state | `ok: true`, `primary_workflow_ready: true`, Neo4j reachable |
| Operational/live state | `operational_live_ready: false`, `operational_refresh_required: true`, freshness `unknown` |
| Optional operation | The core journey and deterministic report passed without an optional model or live-source credential. |
| Score status | **Blocked report — 78.7/101. Not a perfect rubric pass.** |

The frozen six-criterion contract and direct scoring rules are in
[README.md](README.md). Raw weights remain **30/25/20/15/10/1 = 101** and are
not normalized.

## Pass record

V4 replaces only the current-evidence pointer after the shared-main navigation
merge. V1–v3 remain immutable records of the trees judged in those passes.

| Evidence | Pass 1 | Pass 2 |
|---|---|---|
| New Chromium profile | PASS | PASS |
| Browser-origin mission entry | PASS | PASS |
| Rendered, program-scoped supply action | PASS | PASS |
| Loaded graph result rather than static chrome | PASS | PASS |
| Program-scoped assessed portfolio | PASS | PASS |
| Technical journey duration | 13s / 360s | 12s / 360s |

These are two consecutive **technical journey** passes, not two 101-point
rubric passes.

Supporting evidence:

- Complete backend suite: PASS; **519 passed, 18 skipped**, with skips confined
  to optional live-source checks and one dependency deprecation warning.
- Project gate: PASS; BOM valid with **29 data sources and 236 software
  components**, and all 17 synchronization tests passed.
- Web gate: PASS; **47 tests** passed, including route-context, navigation,
  latest-request, mission-analysis, graph-scope, and theme regressions. The
  production build passed with the existing non-fatal mixed-import warning for
  `SourceFrame.vue`.
- Focused assembled gate: PASS; **123 passed, 16 optional-live checks skipped**
  across sanctions, report indicators/ownership, claim boundaries, supply-only
  portfolio scope, and live-source output contracts.
- Rebase conflict contract: PASS. The assembled Claims view retains simplified,
  URL-authoritative navigation and stale-request protection together with
  claim-ID/entity/program filtering, rationale submission, and accountability
  history.
- Cross-source sanctions behavior: PASS. A clear from one source cannot conceal
  a committed hit from another; source/detail/evidence references are retained.
- UN list boundary: PASS. Invalid envelopes, malformed XML, missing generation
  metadata or sections, and empty entity/person coverage cannot emit an
  authoritative clear.
- Mission portfolio scope: PASS. Only bounded `SUPPLIES` paths determine
  membership; ownership-parent lookup decorates selected members.
- Live assembled report: PASS. The response preserved `control.ownership` and
  normalized returned affiliation `org_types` values to arrays.
- Negative rehearsal: PASS; the harness rejected an intentionally stalled
  mission template at the expected stage.
- Mission performance: PASS; all ten operations stayed below 5,000 ms.
  Observed maxima were 1.03 ms readiness, 150.85 ms program load, 99.79 ms
  vendor preset, 167.12 ms category preset, 91.03 ms country preset, 72.68 ms
  ownership preset, 79.68 ms sole-source preset, 56.75 ms vendor report,
  213.19 ms comparison, and 37.35 ms export.
- Replit exposure: PASS; `.replit` contains no public mapping for Neo4j ports
  7474 or 7687.
- Visible inspection: the [mission entry](8e0d93b-mission-desktop.jpg) and
  [claim evidence table](8e0d93b-claims-desktop.jpg) rendered at 1280×720 with
  the simplified intent-grouped navigation and no browser-console error.
- Runtime inspection: the workflow remained running. Initial restart health
  retries recovered; expected Neo4j schema notifications for optional fixture
  properties/relationships did not interrupt requests.

## Criterion scores

| Criterion | Raw | Awarded | Confidence | Evidence and deduction |
|---|---:|---:|---|---|
| Mission Impact | 30 | **25** | High for deterministic demo; low for live operational coverage | Clean journeys reached a scoped supply finding and assessed portfolio, and the mission UI states its acquisition question and evidence boundary. Deduct **5** because health still reports unknown freshness and `operational_live_ready: false`; current live operational coverage is not demonstrated. Owned by #69, #71, #79, #107, #108, #75 and #76. |
| Technical Innovation | 25 | **23** | High | Tests and live responses corroborate graph lineage, deterministic scoring, bounded queries, cross-source sanctions aggregation, fail-closed list coverage, optional-service behavior, and exports. Deduct **2** because the automated journey still does not prove every material decision path against current claim/artifact truth after failures and later decisions. Owned by #66, #84, #86 and #90. |
| Usability & Design | 20 | **14** | High for desktop happy path; low for assistive/mobile paths | Navigation now groups intent, preserves URL context, restores focus/control, and survives asynchronous route changes; two clean desktop paths complete under six minutes. Deduct **6** because the full keyboard, focus, screen-reader, narrow-width, 200% zoom, both-theme, and complete-download recovery matrices remain incomplete. Owned by #50, #59–#61, #64, #75–#78 and #84. The navigation merge alone does not close the aggregate deduction. |
| Security & Sustainability | 15 | **12** | High for tested development controls; low for production assurance | Backend tests, fail-closed source coverage, BOM validation, secret/write controls, review accountability, report normalization, and removal of public Neo4j mappings are inspectable. Deduct **1** because one clean restart does not close the retained-state recovery race (#43), **1** for the unproved private-network custom-model boundary (#72), and **1** for unresolved license/pricing and production-hardening decisions (#110). |
| Team Collaboration | 10 | **4** | Low without human evidence | The assembled branch includes refreshed shared main, and repository guidance, subsystem boundaries, presenter script, and reconciliation plan are inspectable. Deduct **6** because no two-presenter spoken timing, named ownership sign-off, fallback recording, or live handoff evidence exists for this revision. Owned by #21 and #28. |
| Interoperability | 1 | **0.7** | High for API; low for organizer publication | Versioned machine-readable findings and lineage contracts work. Deduct **0.2** because the safe complete/bounded export is not yet demonstrable in-product, and **0.1** because no organizer publication receipt or remote dataset identity exists. Owned by #64. |
| **Raw total** | **101** | **78.7** | — | **22.3 points remain unawarded. No normalization applied.** |

## Deduction register

No point changed merely because navigation merged, a regression was repaired,
or a remediation task exists.

| ID | Points | Reproduction or missing evidence | Existing owner |
|---|---:|---|---|
| D1 | 5 mission | `GET /api/health?refresh=true` reports operational live readiness false and freshness unknown. | #69, #71, #79, #107, #108, #75, #76 |
| D2 | 2 innovation | The clean harness proves mission → graph → portfolio, not every factor through current claim/artifact truth after failures and decisions. | #66, #84, #86, #90 |
| D3 | 6 usability | Required keyboard, focus, responsive, zoom, theme, accessible-name, and complete-download checks lack a current complete pass. | #50, #59–#61, #64, #75–#78, #84 |
| D4 | 1 security/sustainability | Repeat retained-state startup enough to close the prior credential-recovery shutdown race; one clean restart is insufficient. | #43 |
| D5 | 1 security/sustainability | The custom-model URL check has not demonstrated its public-network boundary. | #72 |
| D6 | 1 security/sustainability | BOM/legal documentation retains unresolved license and pricing review flags. | #110 |
| D7 | 6 collaboration | Spoken two-presenter timing, ownership sign-off, fallback recording, and live handoffs remain human evidence gaps. | #21, #28 |
| D8 | 0.2 interoperability | Export works through the API but lacks the safe in-product complete/bounded workflow. | #64 |
| D9 | 0.1 interoperability | Catalog dry-run behavior is tested, but no successful organizer receipt or remote dataset identity exists. | #64 |

## Explicit blockers and stop decision

The loop stops honestly at **78.7/101**:

1. Wait for deduction owners to merge and rejudge their behavior; queued or
   merged work earns no credit by itself.
2. Decide whether judged mission impact requires live operational source
   coverage or remains a deterministic fixture-backed demonstration.
3. Produce the human two-presenter rehearsal and handoff evidence.
4. Produce organizer publication evidence or retain validated-dry-run language.
5. Complete the assistive, responsive, theme, zoom, recovery, and download
   browser matrices.
6. Refresh the immutable product revision/tree and rerun targeted checks plus
   the entire rubric after each remediation wave.

Only two consecutive complete passes with current evidence for all six criteria
may record **101/101**.

## Exact rerun commands

```bash
git rev-parse HEAD
git show -s --format=%T HEAD
scripts/rehearse-demo.sh
REHEARSAL_OUTPUT_DIR=/tmp/illuminate-rubric-pass-2 scripts/rehearse-demo.sh
scripts/test-rehearse-demo.sh
make test
npm --prefix web test -- --run
npm --prefix web run build
cd api && uv run --extra dev --frozen pytest -q
python3 scripts/measure-mission-performance.py --environment replit
```

Also inspect `/api/health?refresh=true`, `/api/exports/v1/schema`, a populated
finding page, one assembled entity report, browser/runtime logs, the complete
browser matrix, and the human/organizer evidence above.