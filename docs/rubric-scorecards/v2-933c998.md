# NDIA hostile rubric scorecard — v2

## Evaluation identity

| Field | Recorded value |
|---|---|
| Review date | 2026-09-08 |
| Evaluated revision | `933c998a7aacac9570d8de242e43915d38e93074` |
| Evaluated branch | `replit-configuration` |
| Main-revision confidence | High for ancestry, blocked for publication. The evaluated revision descends from the refreshed shared-main commit `ed0914c40e74b6e6ee80cfc69c5c84d56ded2bf1` through the immutable v1 scorecard commit. It is not represented as merged shared main until this task merges. |
| Runtime | Configured `UC7 Illuminate` Replit development workflow; Neo4j 5.26.1, FastAPI, Vite on port 8080 |
| Published deployment | No immutable production revision was established. No production credit is inferred. |
| Data state | Deterministic fixture seed with 901 committed fixture files; scenario data enabled |
| Required runtime state | `ok: true`, `primary_workflow_ready: true`, Neo4j reachable |
| Operational/live state | `operational_live_ready: false`, `operational_refresh_required: true`, freshness `unknown` |
| Optional operation | The core journey and deterministic report passed without an optional model. |
| Score status | **Blocked report — 78.7/101. Not a perfect rubric pass.** |

The frozen contract and direct six-criterion scoring rules are in
[README.md](README.md). Raw weights remain **30/25/20/15/10/1 = 101** and are
not normalized.

## Pass record

This pass re-evaluated the repaired source tree that was then frozen exactly as
the evaluated revision above. Documentation and evidence images were added
afterward; they do not change the evaluated product behavior.

| Evidence | Pass 1 | Pass 2 |
|---|---|---|
| New Chromium profile | PASS | PASS |
| Browser-origin mission entry | PASS | PASS |
| Rendered, program-scoped supply action | PASS | PASS |
| Loaded graph result rather than static chrome | PASS | PASS |
| Program-scoped assessed portfolio | PASS | PASS |
| Technical journey duration | 15s / 360s | 9s / 360s |

These are two consecutive **technical journey** passes, not two 101-point rubric
passes. The original evidence blockers remain current.

Supporting evidence:

- Configured workflow restart: PASS on the observed restart. One clean attempt
  does not prove the previously observed recovery race is fixed.
- Complete backend suite: PASS; **421 passed, 2 skipped**, with one dependency
  deprecation warning.
- Project gate: PASS; BOM valid with **28 data sources and 236 software
  components**, and all 17 synchronization tests passed.
- Production web build: PASS; 527 modules transformed. The existing non-fatal
  mixed-import warning for `SourceFrame.vue` remains.
- Targeted report, ownership, affiliation, and claim-boundary suite: PASS; 38
  tests.
- Live assembled report: PASS. The entity report returned JSON, preserved the
  `control.ownership` array contract, and represented every returned
  `org_types` value as an array.
- Claim accountability surface: PASS for production build and visible committed
  claims. Rationale submission and entity history contracts are covered by
  executable tests; no claim was mutated merely to capture evidence.
- Negative rehearsal: PASS; the harness rejected an intentionally stalled
  mission template at the expected stage.
- Mission performance: PASS; all ten operations remained below the 5,000 ms
  budget. Observed maxima were 1.35 ms readiness, 61.41 ms program load,
  79.59 ms vendor preset, 129.29 ms category preset, 86.96 ms country preset,
  71.30 ms ownership preset, 62.41 ms sole-source preset, 47.40 ms vendor
  report, 128.32 ms comparison, and 42.82 ms export.
- Replit exposure check: PASS; `.replit` contains no public mappings for Neo4j
  ports 7474 or 7687, so credential recovery cannot be forwarded through those
  mappings.
- Visible inspection: the [mission entry](933c998-mission-desktop.jpg) and
  [claim evidence table](933c998-claims-desktop.jpg) rendered at 1280×720 with
  no browser-console error on the captured routes.
- Runtime logs: the workflow remained running. Neo4j emitted expected schema
  notifications for optional relationship types/properties; no application
  crash was observed.

## Criterion scores

| Criterion | Raw | Awarded | Confidence | Evidence and deduction |
|---|---:|---:|---|---|
| Mission Impact | 30 | **25** | High for deterministic demo; low for live operational coverage | The clean journey reached a scoped supply finding and assessed portfolio, and the mission UI states the acquisition question and evidence boundary. Deduct **5** because health still reports unknown freshness and `operational_live_ready: false`; current live operational coverage is not demonstrated. Owned by #69, #71, #79, #107, #108, #75 and #76. |
| Technical Innovation | 25 | **23** | High | Tests and live response corroborate graph lineage, ownership contracts, deterministic scoring, bounded queries, optional-service behavior, and exports. Deduct **2** because the automated journey still does not prove every material decision path against current claim/artifact truth after failures and later decisions. Owned by #66, #84, #86 and #90. |
| Usability & Design | 20 | **14** | High for desktop happy path; low for assistive/mobile paths | Mission hierarchy and the claims table render clearly, and two clean desktop paths complete under six minutes. Deduct **6** because keyboard, focus, screen-reader, required narrow-width, 200% zoom, both-theme, and complete-download recovery evidence remains incomplete. Owned by #50, #59–#61, #64, #75–#78 and #84. |
| Security & Sustainability | 15 | **12** | High for tested development controls; low for production assurance | Backend tests, fault boundaries, BOM validation, secret/write controls, restored accountability UI, assembled API normalization, and removal of public Neo4j mappings are inspectable. Deduct **1** because one clean restart does not close the retained-state recovery race (#43), **1** for the unproved private-network custom-model boundary (#72), and **1** for unresolved license/pricing and production-hardening decisions (#110). |
| Team Collaboration | 10 | **4** | Low without human evidence | Git ancestry now includes refreshed shared main, and repository guidance, subsystem boundaries, presenter script, and reconciliation plan are inspectable. Deduct **6** because no two-presenter spoken timing, named ownership sign-off, fallback recording, or live handoff evidence exists for this revision. Owned by #21 and #28; merge publication must still establish the task revision on shared main. |
| Interoperability | 1 | **0.7** | High for API; low for organizer publication | Versioned machine-readable findings and lineage contracts work. Deduct **0.2** because the safe complete/bounded export is not yet demonstrable in-product, and **0.1** because no organizer publication receipt or remote dataset identity exists. Owned by #64. |
| **Raw total** | **101** | **78.7** | — | **22.3 points remain unawarded. No normalization applied.** |

## Deduction register

No point changed because a task exists or because an unrelated regression was
repaired.

| ID | Points | Reproduction or missing evidence | Existing owner |
|---|---:|---|---|
| D1 | 5 mission | `GET /api/health?refresh=true` still reports operational live readiness false and freshness unknown. | #69, #71, #79, #107, #108, #75, #76 |
| D2 | 2 innovation | The clean harness proves mission → graph → portfolio, not every factor through current claim/artifact truth after failures and decisions. | #66, #84, #86, #90 |
| D3 | 6 usability | Required keyboard, focus, responsive, zoom, theme, accessible-name, and complete-download checks lack a current complete pass. | #50, #59–#61, #64, #75–#78, #84 |
| D4 | 1 security/sustainability | Repeat retained-state startup enough to close the prior credential-recovery shutdown race; one observed clean restart is insufficient. | #43 |
| D5 | 1 security/sustainability | The custom-model URL check has not demonstrated its public-network boundary. | #72 |
| D6 | 1 security/sustainability | BOM/legal documentation intentionally retains unresolved license and pricing review flags. | #110 |
| D7 | 6 collaboration | Spoken two-presenter timing, ownership sign-off, fallback recording, and live handoffs remain human evidence gaps. | #21, #28 |
| D8 | 0.2 interoperability | Export works through the API but lacks the safe in-product complete/bounded workflow. | #64 |
| D9 | 0.1 interoperability | Catalog dry-run behavior is tested, but no successful organizer receipt or remote dataset identity exists. | #64 |

## Explicit blockers and stop decision

The loop stops honestly at **78.7/101**:

1. Merge the evaluated task revision before representing it as shared main.
2. Wait for the deduction owners to merge; queued work earns no credit.
3. Decide whether judged mission impact requires live operational source
   coverage or remains a deterministic fixture-backed demonstration.
4. Produce the human two-presenter rehearsal and handoff evidence.
5. Produce organizer publication evidence or retain validated-dry-run language.
6. Complete the assistive, responsive, theme, zoom, recovery, and download
   browser matrices.
7. Refresh the immutable revision and rerun the targeted checks plus the entire
   rubric after each remediation wave.

Only two consecutive complete passes with current evidence for all six criteria
may record **101/101**.

## Exact rerun commands

```bash
git rev-parse HEAD
scripts/rehearse-demo.sh
REHEARSAL_OUTPUT_DIR=/tmp/illuminate-rubric-pass-2 scripts/rehearse-demo.sh
scripts/test-rehearse-demo.sh
make test
npm --prefix web run build
cd api && uv run --extra dev --frozen pytest -q
python3 scripts/measure-mission-performance.py --environment replit
```

Also inspect `/api/health?refresh=true`, `/api/exports/v1/schema`, a populated
finding page, one assembled entity report, browser/runtime logs, the complete
browser matrix, and the human/organizer evidence above.